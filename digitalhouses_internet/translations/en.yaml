configuration:
  router_ip:
    name: Router IP
    description: LAN IP address used to check whether the router itself is reachable.
  connectivity_check:
    name: Connectivity check
  speedtest:
    name: Speedtest
    description: Ookla Speedtest periodic interval and timeout.
  traffic:
    name: Router telemetry
    description: Optional cumulative traffic, WAN state and current rate bindings. All fields may be empty.
  recovery:
    name: Recovery
  log_level:
    name: Log level
